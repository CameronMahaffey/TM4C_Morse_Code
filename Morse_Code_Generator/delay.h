// Declarations of delay functions to give LCD time to finish processing commands and for sending Morse codes

#ifndef __DELAY_H__
#define __DELAY_H__

// Delays in units of microseconds
void delay_us(unsigned short usec);

// Delays in units of milliseconds
void delay_ms(unsigned short msec);

#endif //__DELAY_H__
